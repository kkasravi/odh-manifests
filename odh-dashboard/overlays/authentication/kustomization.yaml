apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - clusterrolebinding.yaml
  - ../../base
commonLabels:
  app: odh-dashboard # {"$openapi":"DASHBOARD_NAME"}
patchesJson6902:
  - path: deployment.patch
    target:
      group: apps
      version: v1
      kind: Deployment
      name: odh-dashboard # {"$openapi":"DASHBOARD_NAME"}
  - path: service-account.patch
    target:
      kind: ServiceAccount
      version: v1
      name: odh-dashboard # {"$openapi":"DASHBOARD_NAME"}
  - path: service.patch
    target:
      kind: Service
      version: v1
      name: odh-dashboard # {"$openapi":"DASHBOARD_NAME"}
  - path: route.patch
    target:
      group: route.openshift.io
      version: v1
      kind: Route
      name: odh-dashboard # {"$openapi":"DASHBOARD_NAME"}
